------------------- FIX 
1. Response Buttons

	VSA 
	In half of the block, RIGHT = T and LEFT = L
	In half of the block, RIGHT = L and LEFT = T

	Calc
	In half of the block, RIGHT = Correct and LEFT = Incorrect
	In half of the block, RIGHT = Incorrect and LEFT = Correct


2. EyeTracker
	
	Check eyetracker version: 
	eyelink_ver = getEYELINK().getTrackerVersion()

	Make sure to have the good parser configuration (eyelink manual)


3. 

