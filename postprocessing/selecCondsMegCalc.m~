function [cond_idx, labels] = selecCondsMegCalc(trialinfo, conds);

if strcmp(conds, 'corrResult')
    (trialinfo.(conds) > 2 & trialinfo.conds < 7 & trialinfo.operator ~= 0);
    labels = trialinfo.(conds)(conds_idx);
    
end

    
    
end

